# local dev — vite proxies /api here
VITE_API_PROXY_TARGET=http://localhost:3000

# optional: call API directly (needs CORS). default is /api/v1 via proxy
# VITE_API_BASE_URL=/api/v1

# docker/nginx — where /api gets proxied
API_UPSTREAM=http://api:3000
