x-backend: &backend
  build: .
  env_file: .env.production.local
  depends_on:
    postgres:
      condition: service_healthy
    redis:
      condition: service_healthy
  restart: unless-stopped

# These override the localhost values from .env.production.local so containers
# reach each other by service name on the compose network.
x-backend-env: &backend-env
  DB_HOST: postgres
  REDIS_HOST: redis
  HOSTNAME: 0.0.0.0

services:
  api:
    <<: *backend
    command: node app.js
    environment:
      <<: *backend-env
      DB_USER: bia_app
    ports:
      - "3000:3000"

  dispatcher:
    <<: *backend
    command: node dispatcher-server.js
    environment:
      <<: *backend-env
      DB_USER: bia_worker

  worker:
    <<: *backend
    command: node worker-server.js
    environment:
      <<: *backend-env
      DB_USER: bia_worker

  frontend:
    build: ./frontend
    environment:
      API_UPSTREAM: http://api:3000
    ports:
      - "8080:80"
    depends_on:
      - api
    restart: unless-stopped

  postgres:
    image: postgres:17-alpine
    env_file: .env.production.local
    environment:
      POSTGRES_USER: bia_user
      POSTGRES_DB: bia_prod
    entrypoint:
      - /bin/sh
      - -c
      - |
        if [ -z "$$DB_PASSWORD" ]; then
          echo "DB_PASSWORD is required for postgres startup" >&2
          exit 1
        fi
        export POSTGRES_PASSWORD="$$DB_PASSWORD"
        exec docker-entrypoint.sh postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bia_user -d bia_prod"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:8-alpine
    env_file: .env.production.local
    command:
      - /bin/sh
      - -c
      - |
        if [ -z "$$REDIS_PASSWORD" ]; then
          echo "REDIS_PASSWORD is required for redis startup" >&2
          exit 1
        fi
        exec redis-server --requirepass "$$REDIS_PASSWORD"
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$$REDIS_PASSWORD\" ping | grep -q PONG"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  pgadmin:
    image: dpage/pgadmin4
    env_file: .env.production.local
    ports:
      - "5050:80"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
